version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    depends_on:
      - database

  database:
    image: mongo:6
    restart: always
    volumes:
      - mongo_data:/data/db

  
volumes:
  mongo_data: